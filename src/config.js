// Environment-specific configuration
const config = {
  // API base URL - use the Render deployment URL
  apiUrl: "https://server-forum.onrender.com/api",

  // Authentication storage keys
  tokenKey: "token",
  userKey: "user",
}

export default config


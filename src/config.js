// Environment-specific configuration
const config = {
  // API base URL - use the Render deployment URL with the correct format
  apiUrl: "https://server-forum.onrender.com/api", // Updated to match your actual API URL

  // Authentication storage keys
  tokenKey: "cookforum_token",
  userKey: "cookforum_user",
}

export default config
